<Window
    x:Class="LF08Nr2.View.ImportView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModels="clr-namespace:LF08Nr2.ViewModel"
    xmlns:local="clr-namespace:LF08Nr2.View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Import"
    WindowStartupLocation="CenterScreen"
    Width="800"
    Height="450"
    MinWidth="400"
    MinHeight="400"
    Background="{DynamicResource GrayBrush}"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
           <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button
            Grid.Row="1"
            Grid.Column="0"
            Grid.RowSpan="3"

            Width="200"
            Height="80px"
            Margin="20"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            FontSize="20"
            FontWeight="Bold"
            Content="Datein auswählen" 
            Command="{Binding Path=CoolCommand}"/>
        <!--Click="AddDater"
            Command="{Binding clickCommand, RelativeSource={RelativeSource AncestorType=UserControl, Mode=FindAncestor}}"-->

        <Button
            Grid.Row="3"
            Grid.Column="2"
            Width="100"
            Height="40px"
            Margin="20"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Click="CloseWindow"
            Content="Schließen" />

        <StackPanel
            Grid.Row="1"
            Grid.Column="3"
            Grid.RowSpan="2"
            Width="200"
            Height="30px"
            Margin="30"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">

            
            <StackPanel.Resources>
                <viewModels:CoolVacation x:Key="mewtooVacation"/>
            </StackPanel.Resources>
            
            
            <ComboBox 
            Name="cooleComboBox"
            ItemsSource="{StaticResource mewtooVacation}"
            Text="coolShit"
            IsEditable="True"
            IsReadOnly="True"/>

            <TextBlock Text="{Binding ElementName=cooleComboBox, Path=SelectedItem}"/>
        </StackPanel>

        <!--<DataGrid
            Grid.Row="1"
            Grid.Column="0"
            Margin="10"
            ItemsSource="{Binding Path=(viewModels:ImportViewModel.Files), UpdateSourceTrigger=PropertyChanged}"
            Background="{DynamicResource ListBackground}" 
            Name="TextBlock"
            />

        <DataGrid
            Name="DummyName"
            Grid.Row="1"
            Grid.RowSpan="2"
            Grid.Column="1"
            Margin="10"
            Background="{DynamicResource ListBackground}" />-->

    </Grid>
</Window>
